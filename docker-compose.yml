services:
  rss-bot:
    build: .
    container_name: rss-bot
    restart: unless-stopped

    volumes:
      - ./data:/app/data

    environment:
      CHANNEL_ID: ""

    secrets:
      - telegram_token

secrets:
  telegram_token:
    file: ./secrets/telegram_token.txt
